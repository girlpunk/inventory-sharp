@if (Exception is not null) {
    <Alert Type="AlertType.Warning" Description="@Title" @Message="@Exception.Message"/>
}

@code {
    [Parameter] public string Title { get; set; } = "Error!";
    [Parameter] public Exception? Exception { get; set; }
}
