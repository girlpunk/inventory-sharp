@using Microsoft.AspNetCore.Identity
@using BlazorInventory.Data

@inject SignInManager<ApplicationUser> SignInManager

<Menu>
    <MenuItem Path="Account/Manage">Profile</MenuItem>
    <MenuItem Path="Account/Manage/Email">Email</MenuItem>
    <MenuItem Path="Account/Manage/ChangePassword">Password</MenuItem>
    @if (_hasExternalLogins)
    {
        <MenuItem Path="Account/Manage/ExternalLogins">External logins</MenuItem>
    }
    <MenuItem Path="Account/Manage/TwoFactorAuthentication">Two-factor authentication</MenuItem>
    <MenuItem Path="Account/Manage/PersonalData">Personal data</MenuItem>
</Menu>

@code {
    private bool _hasExternalLogins;

    protected override async Task OnInitializedAsync()
    {
        _hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();
    }
}
